# Errata and supersessions — 2026-08-13

This file records corrections to earlier notes on the branch without rewriting their historical proof-search context.

## Lean theorem namespace

`NS_BOUNDED_RESERVOIR_FIXED_CUBIC_WINDOW_2026-08-13.md` names the pressure-recurrence theorem as

`NavierStokesPressureRecurrence.cubicEventInEveryAdjacentPair`.

The standalone helper module was subsequently inlined into the workflow entrypoint. The verified theorem's current full name is

`NavierStokesCoveringCountermodel.cubicEventInEveryAdjacentPair`.

The mathematical statement is unchanged.

## Workflow scope

Earlier PR text said no workflow file had changed. That became stale after adding a second finite countermodel file. Commit `4c86f1a4e56506c36e54e8425dc52850ad4657d2` strengthens the existing workflow so it trust-scans and compiles every `*.lean` file in the verification directory. It does not weaken any trust rule.

## Threshold-route status

Earlier notes described the first-threshold architecture as conditionally repairable by four source-interface theorems. The later audit of arXiv:2605.09797v2 found an independent downstream obstruction in Propositions 5.2–5.3: quotient equality is used as unconstrained variational stationarity, and a fixed-cutoff localized functional is used as though it were exactly homogeneous under dilation.

Therefore the current superseding verdict is:

- a uniform same-scale seed can repair only the scalar absorption step;
- a selected-interval depletion theorem can conditionally repair only cross-scale exhaustion;
- neither repairs the terminal strictness theorem;
- the printed v2 route additionally requires `NS-CONSTRAINED-ENDPOINT-POHOZAEV`, including admissible variations, all constraint multipliers, and cutoff/collar derivative terms.

No official Navier–Stokes theorem is claimed.
