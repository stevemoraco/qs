# Expected result

Ten staged theorem declarations must compile under Lean/Mathlib 4.32.1 with no forbidden proof carrier and with every printed axiom set contained in `propext`, `Classical.choice`, and `Quot.sound`.
