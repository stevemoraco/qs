# Millennium Braid Everything — one-file hostile replay

Date: 2026-08-13 UTC

Status: **single generated Lean source containing the recovered trust-clean finite theorem bank; official Millennium conclusions remain conditional on explicit native bridges; six-alarm off.**

## Exact source

The workflow reconstructs one immutable Lean source from the committed base64/xz shards:

```text
file:    MillenniumBraidEverything.lean
bytes:   681395
lines:   9963
SHA-256: 0b5991423ffce0f4c55ece074d530ba0dd50c132b3de8698d3bb1de55257327c
```

Compressed transport identity:

```text
bytes:   83372
SHA-256: 52ec1001a3ee33b33e0ad1c1a024d572396850dd3c6a926299da45a6b2664578
```

The source contains 832 explicit `#print axioms` commands and 496 physically present `theorem`/`lemma` declarations. The generated inventory records:

```text
134 source-file occurrences
970 named declaration occurrences
717 theorem/lemma occurrences
662 trust-clean theorem/lemma occurrences
491 unique trust-clean materialized proof bodies
```

The numbers are intentionally distinguished. `717` is an indexed occurrence count, not a claim of 717 unique kernel-checked proof bodies. The physical materialized bank contains 491 unique recovered trust-clean proof bodies, plus the generated inventory and braid-interface theorems.

## Included interfaces

The single source contains finite formal cores and explicit target interfaces for:

1. Riemann Hypothesis;
2. P versus NP;
3. Birch and Swinnerton-Dyer;
4. Hodge;
5. Navier–Stokes;
6. Yang–Mills mass gap;
7. the Perelman/Poincaré completion chain;
8. the cross-problem seventh-object inversion interface.

## Final executable statement

The terminal declaration is:

```lean
MillenniumBraidEverythingExecutable.millenniumBraidEverythingExecutable
```

It accepts

```lean
(S : MillenniumBraidUnified.OfficialStatements)
(B : MillenniumBraidUnified.CompleteBraid S)
```

and returns the finite bank, all eight target propositions, absence of every finite inversion defect, and the exact source/declaration/theorem inventory.

The argument `B : CompleteBraid S` is visible and load-bearing. It packages the native problem-sized bridges that remain mathematically unresolved. The source does not construct `B`, does not conceal it in an axiom, and does not turn the conditional theorem into a Clay solution.

## Hosted verification contract

The public workflow:

- checks out the exact branch head rather than a mutable default branch;
- reconstructs and hash-checks the exact 681395-byte source;
- performs a comment-aware trust scan rejecting `sorry`, `admit`, `sorryAx`, custom `axiom`, `opaque`, `unsafe`, `native_decide`, and `Lean.ofReduceBool` in executable code;
- pins Lean and Mathlib `v4.32.1`;
- compiles the complete source in one invocation;
- parses all 832 emitted axiom reports;
- requires the final executable theorem and the Perelman/inversion braid theorem to appear in those reports;
- permits only `propext`, `Classical.choice`, and `Quot.sound`;
- preserves source, compiler output, manifests, audits, and commit identity as a replay artifact.

## Honesty boundary

A green workflow proves that the recovered finite theorem bank and the conditional unified statement compile together under the Lean kernel. It does **not** prove any of the six unsolved Clay problems. The remaining task is to construct the visible native bridge data without assuming the target, using custom axioms, or importing an unresolved theorem under another name.

No official alarm may be raised from this artifact alone.
