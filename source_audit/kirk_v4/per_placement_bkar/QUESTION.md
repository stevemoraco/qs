# Fixed-root per-placement BKAR audit

Determine whether Theorems 4.4 and 4.6 actually prove, for each fixed placement of at most two passive exterior roots, the same parent-normalized nonroot child factor `m < 1`, or whether the later U5 statement silently assumes that extension.

Trace every definition and later use of passive/exterior/retained roots and connector rows. In particular, determine whether the source ever defines a passive root as a bounded local insertion whose norm is independent of volume, replicas, forest parameters, exterior data, cutoff, and preceding weak depth.

Audit separately: root exclusion from child count; endpoint/hard-core derivative ownership; polynomial placement count; fixed-placement operator norm; and transport through localization/collect. C122 already closes the polynomial placement sum once a per-placement estimate is supplied.
