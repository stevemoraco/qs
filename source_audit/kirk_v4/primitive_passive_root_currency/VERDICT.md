# Kirk v4 primitive passive-root currency audit

Date: 2026-08-17

Status: **🧱 source/dependency gap isolated · 🔵 finite Lean firewall separately verified · analytic Yang–Mills theorem not verified · FIVE-ALARM OFF**

## Exact question

Does the pinned Kirk-v4 manuscript prove that every primitive weak-scale passive exterior-root insertion has one cutoff-, volume-, exterior-, scale-, and preceding-depth-uniform analytic/rooted atom norm in the exact currency consumed by Theorem 4.4 and Theorem 5.27(U5)?

This is logically earlier than:

- fixed one/two-root placement counting;
- root-preserving localization/collect;
- the zero/one/two-root triangular recursion; and
- terminal matching-scale two-root estimates.

## Immutable source and replay

Pinned source: Kirk v4, Zenodo record `21765806`.

PDF SHA-256:

```text
c78a3ce6d273ce7e2d32ecd2cf796a81d1f16160aadc3231752c9dbf65a6befa
```

Exact sector extraction:

```text
source branch    verification/ym-kirk-v4-section7-extract-20260816-gpt56pro
commit           d5e1b5bf5d3129ce96ea0c5caba9d05c68f66dfa
run/job          31982078335 / 95250544387
conclusion       success
artifact         9272643474
artifact digest  sha256:6cb270eeddf1ac77cb30b947aeb7515e87f84172db53855f0f8a3812dbc60d09
```

A fresh local download matched the artifact digest exactly. The extractor located 269 theorem-like headings and exact contexts for the full weak/compact/source chain. Its root-claim scan found:

```text
root declarations:          2
primitive/source atom prose:1
root-row phrases:           5
root-preservation phrase:   1
weak root-analytic phrases: 0
```

The only `ROOT_ANALYTIC` hit in the full source is Lemma 6.22's **compact matching** pivot–optical–source tube, not a primitive weak-scale passive-root theorem.

## Source reconstruction

### 1. Section 4.2 declares roots but does not put them in the displayed norm

Section 4.2 says an activity carries:

- active structural multi-index `alpha`, `|alpha|<=3`;
- passive bounded-source multi-index `I`, `|I|<=N`; and
- at most two passive exterior roots.

But the displayed norm is

```text
sup_e sum_{X contains e} exp(a|X|)
  max_{|alpha|<=3, |I|<=N}
  sigma_s^{|I|}/|I|! * ||D_str^alpha D_J^I F_X||.
```

It contains structural and source derivatives only. No root variable, root seminorm, root normalization, root-buffer row, or primitive root-atom constant appears in the formula.

A declaration that an object may carry roots is not a norm estimate for their amplitudes.

### 2. Theorem 4.6 proves source-leaf rows, not primitive exterior-root rows

Theorem 4.6 extends the weak domain to arbitrary fixed passive-source order. Its proof says source differentiation inserts bounded finite-range atoms, then pays the `V^N` placements of `N` labelled source leaves.

That proof is useful for source derivatives. It does not define the passive exterior-root seminorm or prove a uniform bound for a primitive root insertion. A retained root is not automatically the same object as an integrated source derivative.

### 3. The one-step species ledger remains source-only

Lemma 4.25 supplies a common analytic tube and finite fixed-template derivative suprema through structural order three and every fixed passive-source order.

Proposition 4.26 gives the complete regular atom mass and says the same estimate holds after three structural marks and every fixed passive-source order. It does not add a root row.

Lemma 4.27 and Theorem 4.29 then inherit the active/source inventory. Their printed recursion controls the nonroot/source-marked species mass, but no primitive root amplitude has yet entered the currency.

### 4. Theorem 4.22 is preservation, not creation

Theorem 4.22 says the Loc/collect estimates remain valid with one or two passive exterior roots. Its proof uses one canonical root/history certificate and prevents volume multiplication.

That is a root-preservation theorem conditional on an incoming rooted estimate. It cannot manufacture a primitive root bound omitted at the input.

### 5. Theorem 5.27(U5) has no printed supplier theorem

Theorem 5.27 states that the same domain controls zero-, one-, and two-root connector rows. Its complete proof of item `(U5)` is:

```text
The bi-graded marked theorem gives U5.
```

The repaired full-manuscript origin scan found exactly one occurrence of `bi-graded` and no theorem heading with that name. The explicit Section 4–5 chain supplies root-preserving architecture and source-order estimates, but the primitive root currency is absent from the displayed weak norm and atom ledger.

Thus the unnamed phrase cannot be reconstructed into a complete proof without adding the primitive root theorem below.

### 6. Later compact and Section-8 estimates cannot close the weak input backward

Lemma 6.22 proves a genuine joint pivot–optical–source analytic row for compact matching. Theorems 6.43–6.44 and Section 7 give terminal matching-scale two-root response estimates after the weak corridor has already been traversed.

Lemma 8.4 and Corollary 8.7 cite Corollary 7.4 for weak-scale passive buffers/intermediate roots. That is a terminal-to-intermediate scope jump. A bounded fully composed endpoint response can hide an unbounded intermediate root, as already kernelized in RH-Lean C118.

The Section-8 Riemann source contraction and later exact source cocycle are valuable, but they also presuppose that the initial weak root is defined and bounded in the consumer currency.

## Finite firewall

A separate Lean theorem gives the exact no-free-lunch model:

- exactly two admitted roots;
- every displayed bulk/source row equal to zero;
- identity propagation, hence exact preservation and printed-norm isometry;
- one-vertex two-root placement cost equal to one; and
- primitive root amplitude `cutoff+1`.

Therefore declarations, all fixed source orders, exact preservation, and paid root-placement entropy can coexist with an unbounded primitive root row.

Public guarded replay:

```text
qs PR            #780
branch           verification/ym-c123-primitive-root-currency-firewall-20260817
head             b7e13e11cf56c11e7987b7dc35c2e4e432a282f8
run/job          31982267318 / 95251054950
AXLE             Lean 4.30.0
okay             true
cached_response  false
failed decls     0
errors/warnings  0 / 0
artifact         9272696163
artifact digest  sha256:8de5adbf0f0dd18a55fba614d05554e3be942e16df67d9bff86d4e85f4e79205
source SHA-256   de1101ec1c5665ac3fb4400624e17d2f1cd64aee553aaae3d9231a01f257dfca
axiom union      {propext, Classical.choice, Quot.sound}
```

## Exact missing theorem

```text
YM-KIRK-PRIMITIVE-PASSIVE-ROOT-ATOM-ROW
```

For each primitive weak root type consumed by Sections 4, 5, and 8, define an actual root Banach/seminorm currency and prove one fixed constant `C_root<infinity`, independent of scale, cutoff, volume, exterior data, and preceding weak depth, such that the primitive root insertion lies in the exact Theorem-4.4 rooted species space with row at most `C_root` on one common analytic tube.

The theorem must separately type at least:

1. bounded positive-scale gauge-invariant observable buffers, with Riemann `l1` and `l∞` normalization;
2. normalized finite local curvature-jet/source buffers in the adapted triangular source norm;
3. background/displacement/stress/rotation-defect root marks used before matching; and
4. irregular/bad-core root insertions, if they are part of the same weak U5 claim.

Then, and only then, the already-banked fixed-root placement theorem, triangular root recursion, root-preserving Loc/collect, and six-map composition may propagate the bound to U5.

## INVENTOR -> CRITIC -> REWRITER

### INVENTOR

Add a root variable `R` to the weak species space and define a tri-graded weak norm

```text
||F||_{a;3,N;q},  q=0,1,2,
```

whose `q=0` restriction is the printed norm. Prove the primitive root atoms on the common weak tube. Use the fixed-root Prüfer/placement theorem to extend Theorem 4.4, then apply the already-verified lower-triangular root recursion.

### CRITIC

Do not identify a passive exterior root with a source derivative without an exact map. Do not use Theorem 7.2/Corollary 7.4 backward. Do not hide growing fine-grid incidence inside a phrase such as “fixed buffer”; the Riemann normalization and exact source transport must pay it. Do not let the root constant depend on ultraviolet depth or the endpoint scale.

### REWRITER

The source/dependency frontier is not the whole weak RG and not root placement. It is the primitive root atom row in the exact weak currency.

The corrected chain is:

```text
primitive weak root atom row
  -> fixed one/two-root BKAR placement extension
  -> zero/one/two-root triangular absorption
  -> root-preserving Loc/collect and six-map composition
  -> Theorem 5.27(U5)
  -> exact Section-8 multiscale forest.
```

## Boundary

This audit does not refute Kirk v4 as a whole; it identifies one missing theorem in the printed proof of U5. It does not verify the infinite-dimensional primitive root row, replica–BKAR, the weak RG, multiscale continuum construction, Osterwalder–Schrader reconstruction, physical mass gap, nontriviality, dimensional transmutation, or the Clay theorem.

FIVE-ALARM OFF.
