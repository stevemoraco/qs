# Kirk v4 passive exterior-root semantics correction

Date: 2026-08-17

Status: **🟢 source-origin correction · 🧱 fixed-placement Banach estimate still unproved independently · no Yang–Mills or Clay proof · FIVE-ALARM OFF**

## Correction

The abstract countermodel in qs #780 is valid: a declaration that an object has roots, plus preservation and placement counting, cannot bound an independent omitted root amplitude.

The exact Kirk-v4 prize-facing source forests do **not** instantiate that independent-coordinate model. Their exterior roots are selected source leaves or finite marked-source packets already carried by the passive-source/structural inventory.

Therefore qs #781 and RH-Lean #1737 are too strong if read as claiming that the manuscript lacks every source origin for its actual Section-8 roots. Their finite firewall remains valid, but its source instantiation must be narrowed.

## Immutable semantics replay

Pinned source: Kirk v4, Zenodo record `21765806`.

PDF SHA-256:

```text
c78a3ce6d273ce7e2d32ecd2cf796a81d1f16160aadc3231752c9dbf65a6befa
```

Failed-first run `31982704484` is non-evidence: the extractor completed its substantive scan but a brittle occurrence-count guard expected two exact phrase hits while the source has three. The mathematical extraction was replayed unchanged after removing only that guard.

Repaired replay:

```text
source branch    verification/ym-kirk-v4-section7-extract-20260816-gpt56pro
commit           143213fdc813406967b4c8971287927c27a3a705
run/job          31982764478 / 95252383153
conclusion       success
artifact         9272845735
artifact digest  sha256:46b2768c13ba667a4f9a91c1cff1a54e64e1bc64fa09f4a7f521d4f6e948310c
```

A fresh local download matched the artifact digest exactly.

## Exact source semantics

### 1. Weak roots are part of the marked-source bookkeeping

Section 4.2 fixes a passive bounded-source order and permits at most two passive exterior roots. The norm formula does not display a separate numerical root coordinate. That omission is consistent with the actual downstream semantics: the roots are distinguished leaves/packets whose local coefficient is measured by the existing source or structural derivative currency, while root status changes the connected-tree combinatorics.

### 2. Theorem 4.6 supplies the local source leaves

Theorem 4.6 says source differentiation inserts bounded finite-range atoms and counts `N` labelled source leaves. Thus a root that is a selected source leaf has a local atom source before any root placement is chosen.

This does not prove the fixed-root replica–BKAR estimate by itself; it identifies the root's local coefficient source.

### 3. The Section-8 forest makes the identification explicit

Immediately before Lemma 8.6, the manuscript defines the multiscale source forest by:

```text
A source derivative is a labelled leaf.
```

Lemma 8.6 says the one/two-root bounds control a component while its source leaves remain external. Corollary 8.7 says the two source leaves have a unique lowest common ancestor. Hence the actual Section-8 weak roots are selected source-derivative leaves, not a new unmeasured amplitude coordinate.

### 4. Appendix A confirms source transport controls external roots

Lemma A.11 states explicitly that finite source transport controls the external roots. Theorem A.13 then fixes one passive-source order large enough for the two curvature roots and applies the permanent rooted marked norm to the complete two-root packet.

This is direct source evidence that at least the prize-facing curvature-root sector is meant to be paid by the passive-source inventory rather than by an independent root norm.

### 5. Structural and normalized local roots remain finite marked-source packets

Theorem 5.26 includes source derivatives through fixed order together with zero, one, or two passive exterior roots. Theorems 6.43–6.44 treat finite local source roots and normalized local roots in the permanent finite source-renormalization norm. Stress, displacement, and rotation roots are introduced as local marked-source insertions.

Thus the source-DAG origin is the finite marked-source chain, not an independent primitive root coordinate.

## What remains genuinely missing from an independent proof

The source correction does **not** prove the actual analytic root theorem. The remaining load-bearing step is the fixed-placement Banach estimate:

```text
YM-KIRK-44-TWO-PASSIVE-ROOT-PER-PLACEMENT-BKAR-BOUND
```

For one fixed selection of at most two source/structural leaves, prove in the actual infinite-dimensional replica–BKAR activity spaces that:

- the leaves are realized in the same permanent rooted species currency;
- every covariance/hard-core derivative and local atom map is bounded with one cutoff/volume/exterior/depth-uniform constant;
- the nonroot child factor is unchanged;
- root externality does not hide a growing support-incidence or fine-grid multiplicity; and
- the resulting row survives localization, collect, hull ownership, recertification, and finite source transport without norm drift or duplication.

Once that per-placement theorem is supplied, the already-banked polynomial root-placement envelope and lower-triangular zero/one/two-root recursion close the finite root layer.

Equivalently, the broad independent replay target is:

```text
YM-KIRK-46-422-429-521-526-MARKED-ROOT-BANACH-ANALYTIC-REPLAY
```

## INVENTOR -> CRITIC -> REWRITER

### INVENTOR

Treat exterior roots as distinguished leaves in the existing source/structural derivative algebra. Instantiate Theorem 4.6 at total passive-source order `N+q`, `q<=2`, retain `q` selected leaves externally, prove the fixed-placement BKAR operator estimate, and only then sum their positions using the verified polynomial envelope.

### CRITIC

The words “source leaf” do not automatically prove an infinite-dimensional operator bound. Source differentiation may be bounded after complete integration while a fixed intermediate placement is not. Fine-grid support incidence, covariance-edge derivatives, irregular components, and norm handoffs must be estimated directly. Terminal Section-7 response rows still cannot be used backward.

### REWRITER

Withdraw the independent-root-coordinate instantiation of the primitive-root firewall for the actual Section-8 source forests. Preserve the firewall as a generic typing test. Replace the source-origin gate by the fixed-placement marked-root Banach theorem above.

## Boundary

This correction is source semantics and dependency repair only. It does not verify the fixed-placement root BKAR estimate, Theorems 4.4/4.6/4.22/4.29/5.26, the weak RG, multiscale continuum construction, Osterwalder–Schrader reconstruction, a physical mass gap, nontriviality, dimensional transmutation, or the Clay theorem.

FIVE-ALARM OFF.
