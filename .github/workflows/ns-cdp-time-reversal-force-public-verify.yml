name: NS CDP time-reversal force public verifier

on:
  push:
    branches:
      - agent/ns-cdp-time-reversal-force-public-verifier-20260813
  pull_request:
    paths:
      - 'verification/ns-cdp-time-reversal-force-public/**'
      - '.github/workflows/ns-cdp-time-reversal-force-public-verify.yml'

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - name: Verify trust boundary
        shell: bash
        run: |
          set -euo pipefail
          dir='verification/ns-cdp-time-reversal-force-public'
          file="$dir/NSCDPTimeReversalForceFirewall.lean"
          actual_blob="$(git hash-object "$file")"
          expected_blob='12715f162eb50df806297b1af481374430e48d44'
          test "$actual_blob" = "$expected_blob"
          printf 'canonical_private_blob=%s\n' "$actual_blob" | tee "$dir/source-blob.txt"
          sha256sum "$file" | tee "$dir/source-sha256.txt"
          if grep -En '(^|[^[:alnum:]_])(sorry|admit|sorryAx)([^[:alnum:]_]|$)' "$file"; then
            exit 1
          fi
          if grep -En '^[[:space:]]*(axiom|opaque|unsafe)[[:space:]]' "$file"; then
            exit 1
          fi
          if grep -En '(native_decide|Lean\.ofReduceBool)' "$file"; then
            exit 1
          fi

      - name: Install pinned Lean and compile exact source
        shell: bash
        run: |
          set -euo pipefail
          dir='verification/ns-cdp-time-reversal-force-public'
          curl --proto '=https' --tlsv1.2 -sSf \
            https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh \
            | sh -s -- -y --default-toolchain leanprover/lean4:v4.32.1
          export PATH="$HOME/.elan/bin:$PATH"
          lean --version | tee "$dir/lean-version.txt"
          lake --version | tee -a "$dir/lean-version.txt"
          cd "$dir"
          lake update
          lake exe cache get
          set +e
          lake env lean NSCDPTimeReversalForceFirewall.lean > lean-output.log 2>&1
          status=$?
          set -e
          cat lean-output.log
          if [ "$status" -ne 0 ]; then
            exit "$status"
          fi
          if grep -Eq 'declaration uses .sorry.|sorryAx|admit|Lean.ofReduceBool' lean-output.log; then
            exit 1
          fi
          echo 'VERIFIED NS CDP time-reversal force finite algebra.'

      - name: Upload verification evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ns-cdp-time-reversal-force-public-evidence
          path: |
            verification/ns-cdp-time-reversal-force-public/source-blob.txt
            verification/ns-cdp-time-reversal-force-public/source-sha256.txt
            verification/ns-cdp-time-reversal-force-public/lean-version.txt
            verification/ns-cdp-time-reversal-force-public/lean-output.log
