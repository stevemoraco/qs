name: YM selected observable replay

on:
  push:
    branches:
      - verification/ym-selected-observable-full-gap-firewall-20260813
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - name: Replay
        run: bash verification/ym-selected-observable-full-gap-firewall-20260813/run.sh
