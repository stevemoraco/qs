name: RH critical Chebyshev average public verifier

on:
  push:
    branches:
      - agent/rh-critical-chebyshev-average-public-verifier-20260813
  pull_request:
    paths:
      - 'verification/rh-critical-chebyshev-average-public/**'
      - '.github/workflows/rh-critical-chebyshev-average-public-verify.yml'

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - name: Verify trust boundary
        shell: bash
        run: |
          set -euo pipefail
          dir='verification/rh-critical-chebyshev-average-public'
          file="$dir/RHCriticalChebyshevAverageFinite.lean"
          actual_blob="$(git hash-object "$file")"
          expected_blob='5322a9a90b8292309b519982a46f9a735fe19bb0'
          test "$actual_blob" = "$expected_blob"
          printf 'canonical_private_blob=%s\n' "$actual_blob" | tee "$dir/source-blob.txt"
          sha256sum "$file" | tee "$dir/source-sha256.txt"
          if grep -En '(^|[^[:alnum:]_])(sorry|admit|sorryAx)([^[:alnum:]_]|$)' "$file"; then
            exit 1
          fi
          if grep -En '^[[:space:]]*(axiom|opaque|unsafe)[[:space:]]' "$file"; then
            exit 1
          fi
          if grep -En '(native_decide|Lean\.ofReduceBool)' "$file"; then
            exit 1
          fi

      - name: Install pinned Lean and compile exact source
        shell: bash
        run: |
          set -euo pipefail
          dir='verification/rh-critical-chebyshev-average-public'
          curl --proto '=https' --tlsv1.2 -sSf \
            https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh \
            | sh -s -- -y --default-toolchain leanprover/lean4:v4.32.1
          export PATH="$HOME/.elan/bin:$PATH"
          lean --version | tee "$dir/lean-version.txt"
          lake --version | tee -a "$dir/lean-version.txt"
          cd "$dir"
          lake update
          lake exe cache get
          set +e
          lake env lean RHCriticalChebyshevAverageFinite.lean > lean-output.log 2>&1
          status=$?
          set -e
          cat lean-output.log
          if [ "$status" -ne 0 ]; then
            exit "$status"
          fi
          if grep -Eq 'declaration uses .sorry.|sorryAx|admit|Lean.ofReduceBool' lean-output.log; then
            exit 1
          fi
          echo 'VERIFIED critical Chebyshev average finite algebra.'

      - name: Upload verification evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: rh-critical-chebyshev-average-public-evidence
          path: |
            verification/rh-critical-chebyshev-average-public/source-blob.txt
            verification/rh-critical-chebyshev-average-public/source-sha256.txt
            verification/rh-critical-chebyshev-average-public/lean-version.txt
            verification/rh-critical-chebyshev-average-public/lean-output.log
