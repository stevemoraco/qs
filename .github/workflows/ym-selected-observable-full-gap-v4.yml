name: YM selected observable v4 replay

on:
  push:
    branches:
      - verification/ym-selected-observable-full-gap-firewall-20260813
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - name: Replay v4
        run: bash verification/ym-selected-observable-full-gap-firewall-20260813/run-v4.sh
