name: Verify unified Millennium braid everything

on:
  pull_request:
    paths:
      - ".github/workflows/verify-millennium-braid-everything.yml"
      - "verification/millennium-braid-everything/**"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-22.04
    timeout-minutes: 60
    steps:
      - name: Checkout verifier
        uses: actions/checkout@v4

      - name: Pin mirrored source identity and reject proof carriers
        working-directory: verification/millennium-braid-everything/project
        run: |
          set -euo pipefail
          test "$(git hash-object lean-worker/MillenniumBraidAll.lean)" = "e36d28929adfde5c500d878dd11f32201b8c68a2"
          test "$(cat lean-toolchain)" = "leanprover/lean4:v4.31.0"
          python3 - <<'PY'
          import pathlib, re

          def code_only(source: str) -> str:
              out = []
              i = 0
              block = 0
              line = False
              string = False
              while i < len(source):
                  if block:
                      if source.startswith('/-', i):
                          block += 1
                          i += 2
                      elif source.startswith('-/', i):
                          block -= 1
                          i += 2
                      else:
                          out.append('\n' if source[i] == '\n' else ' ')
                          i += 1
                  elif line:
                      if source[i] == '\n':
                          line = False
                          out.append('\n')
                      else:
                          out.append(' ')
                      i += 1
                  elif string:
                      if source[i] == '\\' and i + 1 < len(source):
                          out.extend('  ')
                          i += 2
                      elif source[i] == '"':
                          string = False
                          out.append(' ')
                          i += 1
                      else:
                          out.append('\n' if source[i] == '\n' else ' ')
                          i += 1
                  elif source.startswith('--', i):
                      line = True
                      out.extend('  ')
                      i += 2
                  elif source.startswith('/-', i):
                      block = 1
                      out.extend('  ')
                      i += 2
                  elif source[i] == '"':
                      string = True
                      out.append(' ')
                      i += 1
                  else:
                      out.append(source[i])
                      i += 1
              if block or string:
                  raise SystemExit('unterminated comment or string')
              return ''.join(out)

          forbidden = re.compile(
              r'\b(sorry|admit|sorryAx|native_decide|Lean\.ofReduceBool)\b'
              r'|(?m)^\s*(axiom|opaque|unsafe)\b'
          )
          trusted = {
              'MillenniumBraidAll.lean',
              'BSDToupinHaarInversionFirewall.lean',
              'HodgeRationalIntegralFirewall.lean',
              'NSSelectiveLerayAtom.lean',
              'PNPParityWordInjection.lean',
              'PerelmanCompletionRouteEquivalenceFirewall.lean',
              'RHPureSimpleZeroNoCoercivity.lean',
              'SeventhObjectRouteEquivalenceFirewall.lean',
              'YMJacobsenActivityFirewall.lean',
          }
          files = sorted(pathlib.Path('lean-worker', name) for name in trusted)
          for path in files:
              match = forbidden.search(code_only(path.read_text()))
              if match:
                  raise SystemExit(f'forbidden {match.group(0)!r} in {path}')
          print(f'trust preflight: {len(files)} files')
          PY

      - name: Install pinned Lean
        run: |
          curl --proto '=https' --tlsv1.2 -sSf https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh | sh -s -- -y --default-toolchain none
          echo "$HOME/.elan/bin" >> "$GITHUB_PATH"

      - name: Resolve pinned Mathlib
        working-directory: verification/millennium-braid-everything/project
        run: |
          lake update
          lake exe cache get

      - name: Replay every module and classify the coherent closure
        working-directory: verification/millennium-braid-everything/project
        run: |
          set -euo pipefail
          : > module-replay.tsv
          mkdir -p module-logs
          status=0
          lake build BSDToupinHaarInversionFirewall HodgeRationalIntegralFirewall NSSelectiveLerayAtom PNPParityWordInjection PerelmanCompletionRouteEquivalenceFirewall RHPureSimpleZeroNoCoercivity SeventhObjectRouteEquivalenceFirewall YMJacobsenActivityFirewall
          for src in lean-worker/{MillenniumBraidAll,BSDToupinHaarInversionFirewall,HodgeRationalIntegralFirewall,NSSelectiveLerayAtom,PNPParityWordInjection,PerelmanCompletionRouteEquivalenceFirewall,RHPureSimpleZeroNoCoercivity,SeventhObjectRouteEquivalenceFirewall,YMJacobsenActivityFirewall}.lean; do
            module=$(basename "$src" .lean)
            if lake env lean "$src" > "module-logs/$module.log" 2>&1; then
              if grep -q 'sorryAx' "module-logs/$module.log"; then
                printf 'SORRY_AX\\t%s\\n' "$src" >> module-replay.tsv
                status=1
              else
                printf 'PASS\\t%s\\n' "$src" >> module-replay.tsv
              fi
            else
              printf 'FAIL\\t%s\\n' "$src" >> module-replay.tsv
              status=1
            fi
          done
          cat module-replay.tsv
          if [ "$status" -ne 0 ]; then
            echo "Historical bank is not yet one coherent Lean-4.31 closure."
            exit 1
          fi
          lake build MillenniumBraidAll 2>&1 | tee unified-build.txt
          lake env lean lean-worker/MillenniumBraidAll.lean 2>&1 | tee unified-axioms.txt
          ! grep -q 'sorryAx' unified-axioms.txt

      - name: Classify 138-module expanded candidate
        continue-on-error: true
        working-directory: verification/millennium-braid-everything/project
        run: |
          set -u
          : > expanded-module-replay.tsv
          mkdir -p expanded-module-logs
          status=0
          while IFS= read -r src; do
            [ -n "$src" ] || continue
            slug=$(printf '%s' "$src" | tr '/ ' '__')
            if lake env lean "$src" > "expanded-module-logs/$slug.log" 2>&1; then
              if grep -q 'sorryAx' "expanded-module-logs/$slug.log"; then
                printf 'SORRY_AX\\t%s\\n' "$src" >> expanded-module-replay.tsv
                status=1
              else
                printf 'PASS\\t%s\\n' "$src" >> expanded-module-replay.tsv
              fi
            else
              printf 'FAIL\\t%s\\n' "$src" >> expanded-module-replay.tsv
              status=1
            fi
          done <<'EXPANDED_MODULES'
Millennium/BSD/KugaSatoObjectFirewall.lean
Millennium/Hodge/SecantIntersectionFirewall.lean
Millennium/NavierStokes/AOWindowedColumnScaling.lean
Millennium/NavierStokes/AccumulatingForceFirewall.lean
Millennium/NavierStokes/FiniteCarrierIsolation.lean
Millennium/NavierStokes/FiniteEnergyNoBlowupFirewall.lean
Millennium/NavierStokes/Helical345Core.lean
Millennium/NavierStokes/HelicalStarRotorCore.lean
Millennium/NavierStokes/HyperbolicRelayCore.lean
Millennium/NavierStokes/IsoscelesExteriorRatioCore.lean
Millennium/NavierStokes/IsoscelesExteriorRenormalizationCore.lean
Millennium/NavierStokes/NineDirectionStressCone.lean
Millennium/NavierStokes/NinePolarizationCarrierDesign.lean
Millennium/NavierStokes/StarViscosityEnergyGate.lean
Millennium/PNP/GraphWalkConstraintFirewall.lean
Millennium/RH/ChebyshevPositiveBregmanFinite.lean
Millennium/YangMills/ChiralityAbsorptionFirewall.lean
millennium/p-vs-np/AdversarySpectrumLabelBarrier.lean
millennium/p-vs-np/AlgebraicFiberEmbedding.lean
millennium/p-vs-np/BasisOrientationComplexity.lean
millennium/p-vs-np/ExactThreeCNFCore.lean
millennium/p-vs-np/FiberFloorBarrier.lean
millennium/p-vs-np/FourierPhaseEmbedding.lean
millennium/p-vs-np/IsospectralUniversality.lean
millennium/p-vs-np/LexicographicRangeAvoidance.lean
millennium/p-vs-np/NonlinearProjectionUniversality.lean
millennium/p-vs-np/NormalFormBarrier.lean
millennium/p-vs-np/RestrictionUniversalFiber.lean
millennium/p-vs-np/SpectralCSPLabelBarrier.lean
millennium/p-vs-np/SpectralProofComplexityBarrier.lean
millennium/p-vs-np/StretchNonrange.lean
millennium/rh/TwoThirdsOperatorAlgebra.lean
scratch/b3/CriticalPathExactSlackFinite.lean
scratch/bsd_braid/AdelicRestrictednessFinite.lean
scratch/bsd_braid/BocksteinDefectArea.lean
scratch/bsd_braid/CasselsTatePrimaryFiniteNoGo.lean
scratch/bsd_braid/CrossPrimeRationalCoherenceFinite.lean
scratch/bsd_braid/EulerPlateauStopping.lean
scratch/bsd_braid/FINITE_PRIME_SQUARE_CORE.lean
scratch/bsd_braid/FiniteControlEulerCharacteristicFinite.lean
scratch/bsd_braid/FiniteLevelTorsionGrowth.lean
scratch/bsd_braid/GlobalPositivityExactification.lean
scratch/bsd_braid/JetCapacityObstruction.lean
scratch/bsd_braid/ShapeScaleFiniteDifference.lean
scratch/bsd_braid/UnitBlindness.lean
scratch/hodge_braid/BoundaryVanishingForcesInterior.lean
scratch/hodge_braid/RationalEigenlineDescentFinite.lean
scratch/hodge_braid/SecantIntersectionFatalFinite.lean
scratch/hodge_braid/WeilFieldOrbitGate.lean
scratch/millennium/NAVIER_STOKES_QUARTIC_GAP_COUNTEREXAMPLE.lean
scratch/millennium/NS_MixedSupportCounterexample.lean
scratch/millennium_braid/B3_BSD_KuriharaCurvature.lean
scratch/millennium_braid/B4_QUANTITATIVE_CORES.lean
scratch/millennium_braid/B4_SIGN_CERTIFIED_TRUNCATION.lean
scratch/millennium_braid/BSDCyclicRelaxationRigidity.lean
scratch/millennium_braid/ExcessSlotCharging.lean
scratch/millennium_braid/HodgeProjectorCountermodel.lean
scratch/millennium_braid/NSAOAxialReflection.lean
scratch/millennium_braid/NSThresholdZeroCountermodel.lean
scratch/millennium_braid/PNPMcspPaddingNoGo.lean
scratch/millennium_braid/PNPQuantifierObstructionFinite.lean
scratch/millennium_braid/PNPQuantifierSwapCountermodel.lean
scratch/millennium_braid/PNPRestrictionAveraging.lean
scratch/millennium_braid/YANG_MILLS_POLYMER_SCALAR_CORE.lean
scratch/millennium_braid/YMClaimAuditCounterexamples.lean
scratch/millennium_braid/YMTwoLoopNormalFormFinite.lean
scratch/millennium_braid/YMUniformGapCountermodels.lean
scratch/millennium_braid/run14/PNPUnitMaskBarrierFinite.lean
scratch/millennium_braid/run14/RH4ChristoffelFloorFinite.lean
scratch/millennium_braid/run14/RHGumbelEndpointFinite.lean
scratch/navier_stokes_braid/PositiveScaleNoMinimum.lean
scratch/pvsnp_braid/AffineCodeDualCheckFinite.lean
scratch/pvsnp_braid/BlockUnitSurplusFinite.lean
scratch/pvsnp_braid/CJWPaddingExponentNoGo.lean
scratch/pvsnp_braid/DecodableGeneratorBarrierFinite.lean
scratch/pvsnp_braid/DenseHitterSliceTightFinite.lean
scratch/pvsnp_braid/ExponentialPaddingConservation.lean
scratch/pvsnp_braid/LowDimAffineSparseSupersetFinite.lean
scratch/pvsnp_braid/ProjectionCylinderFloorFinite.lean
scratch/pvsnp_braid/SystematicDenseHitterGateFinite.lean
scratch/rh_braid/AntiDiagonalExteriorFinite.lean
scratch/rh_braid/BivariateExteriorFinite.lean
scratch/rh_braid/BivariateExteriorScalarFinite.lean
scratch/rh_braid/BivariateTwoPointFinite.lean
scratch/rh_braid/CenteredFiniteDifferenceExteriorFinite.lean
scratch/rh_braid/ChebyshevAmplifiedFiniteDifferenceFinite.lean
scratch/rh_braid/ChebyshevCascadeFinite.lean
scratch/rh_braid/ConstantModePoleNeutralFactor.lean
scratch/rh_braid/CorrectedGreenFinite.lean
scratch/rh_braid/Cos2JDensityTransitionNoGo.lean
scratch/rh_braid/Cos2JSupportDensityFinite.lean
scratch/rh_braid/Cos4RationalCore.lean
scratch/rh_braid/CramerDepthExponentFinite.lean
scratch/rh_braid/CramerLpDepthFinite.lean
scratch/rh_braid/CramerShortIntervalDepthFinite.lean
scratch/rh_braid/FejerRieszDepthOptimizerFinite.lean
scratch/rh_braid/HausdorffBinomialConditioningFinite.lean
scratch/rh_braid/HermiteExteriorFinite.lean
scratch/rh_braid/JointMinimaxGreenFinite.lean
scratch/rh_braid/LocalCellEnergyFinite.lean
scratch/rh_braid/PoleNeutralTwoModeArchFactor.lean
scratch/rh_braid/PrimeEnergyFiniteCore.lean
scratch/rh_braid/PrimeErrorSpikePersistenceFinite.lean
scratch/rh_braid/RHGumbelPrimePairCancellationFinite.lean
scratch/rh_braid/RHTentRayFinite.lean
scratch/rh_braid/SupportCostDepthFinite.lean
scratch/rh_braid/SupportOptimalMarginFinite.lean
scratch/rh_braid/SurjectiveNegativeIndexCore.lean
scratch/rh_braid/SurjectivePullbackNegative.lean
scratch/rh_braid/SuzukiShiftCountermodel.lean
scratch/rh_braid/TotalVariationOuterStepFinite.lean
scratch/rh_braid/TranslationExteriorFinite.lean
scratch/rh_braid/TranslationPairDeterminant.lean
scratch/rh_braid/XiHausdorffFinite.lean
scratch/yang_mills_braid/CutoffGapLimitNoGoFinite.lean
scratch/yang_mills_braid/CyclicObservableGapFinite.lean
scratch/yang_mills_braid/FiniteObservableFrameGapCore.lean
scratch/yang_mills_braid/FiniteObservableListNoGo.lean
scratch/yang_mills_braid/FixedNeighborhoodCannotRunToInfinity.lean
scratch/yang_mills_braid/FlagClassFunctionObstructionFinite.lean
scratch/yang_mills_braid/FrameToFineGapCertificateFinite.lean
scratch/yang_mills_braid/MarginalCubicNotStrictLinearContraction.lean
scratch/yang_mills_braid/MarginalRobustFinite.lean
scratch/yang_mills_braid/TotalObservableCommonBaseFinite.lean
scratch/yang_mills_braid/VacuumLeakageGapBudgetFinite.lean
verification/run14/B7QuantitativeCore.lean
verification/run14/HodgeTransfer.lean
verification/run14/MillenniumRun14.lean
verification/run14/NSTriadOutputFrequency.lean
verification/run14/NSVanishingThreshold.lean
verification/run14/PNPExponentRatio.lean
verification/run14/PNPPaddingTradeoff.lean
verification/run14/PNPQuantifierGap.lean
verification/run14/SchurComplementFinite.lean
verification/run14/TempleFinite.lean
verification/run14/TwoScaleFrameFinite.lean
verification/run14/YMAbsoluteDefectGapFirewall.lean
verification/run14/YMGapBudget.lean
EXPANDED_MODULES
          cat expanded-module-replay.tsv
          printf 'PASS=%s FAIL=%s SORRY_AX=%s TOTAL=%s\\n' \
            "$(grep -c '^PASS' expanded-module-replay.tsv || true)" \
            "$(grep -c '^FAIL' expanded-module-replay.tsv || true)" \
            "$(grep -c '^SORRY_AX' expanded-module-replay.tsv || true)" \
            "$(wc -l < expanded-module-replay.tsv)" | tee expanded-summary.txt
          if [ "$status" -eq 0 ]; then
            lake build MillenniumExpandedCandidate 2>&1 | tee expanded-build.txt
            lake env lean lean-worker/MillenniumExpandedCandidate.lean 2>&1 | tee expanded-axioms.txt
            if grep -q 'sorryAx' expanded-axioms.txt; then
              echo "Expanded candidate contains sorryAx."
              exit 1
            fi
          else
            echo "Expanded historical corpus is quarantined pending Lean-4.31 repairs." | tee expanded-build.txt
          fi

      - name: Generate SHA and declaration manifest
        working-directory: verification/millennium-braid-everything/project
        run: |
          python3 - <<'PY'
          import hashlib, json, pathlib, re, subprocess
          root = pathlib.Path('lean-worker/MillenniumBraidAll.lean')
          imports = re.findall(r'^import\s+([^\s]+)', root.read_text(), re.M)
          pat = re.compile(
              r'^\s*(theorem|lemma|def|abbrev|structure|inductive|class|instance)\b',
              re.M,
          )
          rows = []
          theorem_lines = declaration_lines = 0
          trusted = {
              'MillenniumBraidAll.lean',
              'BSDToupinHaarInversionFirewall.lean',
              'HodgeRationalIntegralFirewall.lean',
              'NSSelectiveLerayAtom.lean',
              'PNPParityWordInjection.lean',
              'PerelmanCompletionRouteEquivalenceFirewall.lean',
              'RHPureSimpleZeroNoCoercivity.lean',
              'SeventhObjectRouteEquivalenceFirewall.lean',
              'YMJacobsenActivityFirewall.lean',
          }
          for path in sorted(pathlib.Path('lean-worker', name) for name in trusted):
              data = path.read_bytes()
              kinds = pat.findall(data.decode())
              theorem_lines += sum(k in ('theorem', 'lemma') for k in kinds)
              declaration_lines += len(kinds)
              rows.append({
                  'path': str(path),
                  'git_blob': subprocess.check_output(
                      ['git', 'hash-object', str(path)], text=True
                  ).strip(),
                  'sha256': hashlib.sha256(data).hexdigest(),
                  'bytes': len(data),
                  'declaration_lines': len(kinds),
              })
          report = {
              'canonical_source_commit': '9fa0d584e0cb9a39d800ae9c5538a7b5ae96e4a1',
              'verifier_commit': subprocess.check_output(
                  ['git', 'rev-parse', 'HEAD'], text=True
              ).strip(),
              'root_git_blob': subprocess.check_output(
                  ['git', 'hash-object', str(root)], text=True
              ).strip(),
              'toolchain': pathlib.Path('lean-toolchain').read_text().strip(),
              'explicit_imports': len(imports),
              'curated_lean_files': len(rows),
              'theorem_or_lemma_lines': theorem_lines,
              'declaration_lines': declaration_lines,
              'files': rows,
          }
          pathlib.Path('unified-manifest.json').write_text(
              json.dumps(report, indent=2, sort_keys=True) + '\n'
          )
          print(json.dumps({k:v for k,v in report.items() if k != 'files'},
                           indent=2, sort_keys=True))
          PY

      - name: Upload replay evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: millennium-braid-everything-evidence
          path: |
            verification/millennium-braid-everything/project/unified-build.txt
            verification/millennium-braid-everything/project/unified-axioms.txt
            verification/millennium-braid-everything/project/unified-manifest.json
            verification/millennium-braid-everything/project/module-replay.tsv
            verification/millennium-braid-everything/project/module-logs/
            verification/millennium-braid-everything/project/expanded-build.txt
            verification/millennium-braid-everything/project/expanded-axioms.txt
            verification/millennium-braid-everything/project/expanded-summary.txt
            verification/millennium-braid-everything/project/expanded-module-replay.tsv
            verification/millennium-braid-everything/project/expanded-module-logs/
          if-no-files-found: warn
