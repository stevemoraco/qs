name: Hodge Mostaed Audit Finite Core Lean

on:
  push:
    branches:
      - agent/hodge-mostaed-audit-public-verifier-20260811
    paths:
      - 'verification/hodge-mostaed-audit/**'
      - '.github/workflows/hodge-mostaed-audit-public-verify.yml'
  pull_request:
    branches: [main]
    paths:
      - 'verification/hodge-mostaed-audit/**'
      - '.github/workflows/hodge-mostaed-audit-public-verify.yml'
  workflow_dispatch:

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4

      - name: Reject explicit placeholders and native_decide
        shell: bash
        run: |
          set -euo pipefail
          file=verification/hodge-mostaed-audit/HodgeMostaedAuditFinite.lean
          if grep -En '(^|[^[:alnum:]_])(sorry|admit|native_decide)([^[:alnum:]_]|$)' "$file"; then
            echo 'Forbidden Lean placeholder or native_decide found.' >&2
            exit 1
          fi

      - name: Install pinned Lean, fetch pinned Mathlib cache, and compile
        shell: bash
        run: |
          set -euo pipefail
          curl --proto '=https' --tlsv1.2 -sSf \
            https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh \
            | sh -s -- -y --default-toolchain leanprover/lean4:v4.32.1
          export PATH="$HOME/.elan/bin:$PATH"
          lean --version
          lake --version
          cd verification/hodge-mostaed-audit
          lake update
          lake exe cache get
          lake env lean HodgeMostaedAuditFinite.lean 2>&1 | tee lean-output.log
          if grep -Eq 'declaration uses .sorry.|sorryAx|admit|_native\.native_decide\.ax_' lean-output.log; then
            echo 'Forbidden placeholder or native_decide trust axiom found in compiler output.' >&2
            exit 1
          fi
          echo 'VERIFIED finite Hodge Mostaed audit core.'

      - name: Upload compiler evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: hodge-mostaed-audit-finite-core-evidence
          path: verification/hodge-mostaed-audit/lean-output.log
          if-no-files-found: warn
