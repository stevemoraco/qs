name: NS one-generation finite-core inherited replay

on:
  push:
    branches:
      - agent/ns-onegen-master-diagonal-replay-20260814
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-24.04
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4

      - name: Source trust scan
        shell: bash
        run: |
          set -euo pipefail
          for f in \
            verification/ns-onegen-master-diagonal/NSOneGenMasterDiagonal.lean \
            verification/ns-onegen-master-diagonal/NSClockNoAccel.lean
          do
            if grep -En '(^|[^[:alnum:]_])(sorry|admit|sorryAx)([^[:alnum:]_]|$)' "$f"; then exit 1; fi
            if grep -En '^[[:space:]]*(axiom|opaque|unsafe)[[:space:]]' "$f"; then exit 1; fi
            git hash-object "$f"
            sha256sum "$f"
          done

      - name: Install pinned Lean
        shell: bash
        run: |
          set -euo pipefail
          curl --proto '=https' --tlsv1.2 -sSf https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh | sh -s -- -y --default-toolchain leanprover/lean4:v4.31.0
          export PATH="$HOME/.elan/bin:$PATH"
          mkdir -p /tmp/ns-onegen-master
          cd /tmp/ns-onegen-master
          cat > lakefile.toml <<'EOF'
          name = "NSOneGenMaster"
          version = "0.1.0"
          defaultTargets = ["NSOneGenMaster"]

          [[require]]
          name = "mathlib"
          git = "https://github.com/leanprover-community/mathlib4.git"
          rev = "v4.31.0"

          [[lean_lib]]
          name = "NSOneGenMaster"
          EOF
          echo 'leanprover/lean4:v4.31.0' > lean-toolchain
          lake update
          lake exe cache get
          lean --version

      - name: Compile exact mirrored sources and audit axioms
        shell: bash
        run: |
          set -euo pipefail
          export PATH="$HOME/.elan/bin:$PATH"
          cp verification/ns-onegen-master-diagonal/NSOneGenMasterDiagonal.lean /tmp/ns-onegen-master/NSOneGenMasterDiagonal.lean
          cp verification/ns-onegen-master-diagonal/NSClockNoAccel.lean /tmp/ns-onegen-master/NSClockNoAccel.lean
          cd /tmp/ns-onegen-master
          : > lean-output.log
          lake env lean NSOneGenMasterDiagonal.lean 2>&1 | tee -a lean-output.log
          lake env lean NSClockNoAccel.lean 2>&1 | tee -a lean-output.log
          if grep -Eq 'declaration uses .sorry.|sorryAx|admit|error:' lean-output.log; then exit 1; fi
          python3 - <<'PY'
          import re, pathlib
          text=pathlib.Path('lean-output.log').read_text()
          allowed={'propext','Classical.choice','Quot.sound'}
          reports=re.findall(r"'([^']+)' depends on axioms:\s*\[(.*?)\]",text,re.S)
          reports += [(x,'') for x in re.findall(r"'([^']+)' does not depend on any axioms",text)]
          if len(reports) < 6:
              raise SystemExit(f'expected >=6 axiom reports, found {len(reports)}')
          for th,payload in reports:
              deps={x.strip() for x in payload.split(',') if x.strip()}
              extra=deps-allowed
              if extra: raise SystemExit(f'{th}: nonfoundation {sorted(extra)}')
          print('VERIFIED',len(reports),'axiom reports')
          PY
