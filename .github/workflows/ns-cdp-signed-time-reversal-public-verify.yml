name: NS CDP signed time-reversal public verifier

on:
  push:
    branches:
      - agent/ns-cdp-signed-time-reversal-public-verifier-20260813
  pull_request:
    paths:
      - 'verification/ns-cdp-signed-time-reversal-public/**'
      - '.github/workflows/ns-cdp-signed-time-reversal-public-verify.yml'

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - name: Verify trust boundary
        shell: bash
        run: |
          set -euo pipefail
          dir='verification/ns-cdp-signed-time-reversal-public'
          file="$dir/NSCDPSignedTimeReversalFirewall.lean"
          actual_blob="$(git hash-object "$file")"
          expected_blob='d943e4237bcd45ec18ea9c01dfbf160b908ef204'
          test "$actual_blob" = "$expected_blob"
          printf 'canonical_private_blob=%s\n' "$actual_blob" | tee "$dir/source-blob.txt"
          sha256sum "$file" | tee "$dir/source-sha256.txt"
          if grep -En '(^|[^[:alnum:]_])(sorry|admit|sorryAx)([^[:alnum:]_]|$)' "$file"; then
            exit 1
          fi
          if grep -En '^[[:space:]]*(axiom|opaque|unsafe)[[:space:]]' "$file"; then
            exit 1
          fi
          if grep -En '(native_decide|Lean\.ofReduceBool)' "$file"; then
            exit 1
          fi

      - name: Install pinned Lean and compile exact source
        shell: bash
        run: |
          set -euo pipefail
          dir='verification/ns-cdp-signed-time-reversal-public'
          curl --proto '=https' --tlsv1.2 -sSf \
            https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh \
            | sh -s -- -y --default-toolchain leanprover/lean4:v4.32.1
          export PATH="$HOME/.elan/bin:$PATH"
          lean --version | tee "$dir/lean-version.txt"
          lake --version | tee -a "$dir/lean-version.txt"
          cd "$dir"
          lake update
          lake exe cache get
          set +e
          lake env lean NSCDPSignedTimeReversalFirewall.lean > lean-output.log 2>&1
          status=$?
          set -e
          cat lean-output.log
          if [ "$status" -ne 0 ]; then
            exit "$status"
          fi
          if grep -Eq 'declaration uses .sorry.|sorryAx|admit|Lean.ofReduceBool' lean-output.log; then
            exit 1
          fi
          echo 'VERIFIED NS CDP signed time-reversal finite algebra.'

      - name: Upload verification evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ns-cdp-signed-time-reversal-public-evidence
          path: |
            verification/ns-cdp-signed-time-reversal-public/source-blob.txt
            verification/ns-cdp-signed-time-reversal-public/source-sha256.txt
            verification/ns-cdp-signed-time-reversal-public/lean-version.txt
            verification/ns-cdp-signed-time-reversal-public/lean-output.log
