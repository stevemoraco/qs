name: RH gap-drift finite public verifier

on:
  push:
    branches:
      - agent/rh-robin-energy-gap-drift-verifier-20260813
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: rh-gap-drift-public-${{ github.ref }}
  cancel-in-progress: false

jobs:
  verify:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout exact verifier source
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Canonical-source and trust preflight
        shell: bash
        run: |
          set -euo pipefail
          SRC=verification/rh_robin_energy_gap_drift/RHRobinEnergyGapDrift.lean
          EXPECTED_BLOB=30b239d8d79abf422c164c6262b0f9bd1a282574
          ACTUAL_BLOB=$(git hash-object "$SRC")
          test "$ACTUAL_BLOB" = "$EXPECTED_BLOB"
          python3 - <<'PY'
          import pathlib, re
          p = pathlib.Path('verification/rh_robin_energy_gap_drift/RHRobinEnergyGapDrift.lean')
          source = p.read_text()
          forbidden = re.compile(r'(^|[^A-Za-z0-9_])(sorry|admit|sorryAx|axiom|opaque|unsafe)([^A-Za-z0-9_]|$)')
          match = forbidden.search(source)
          if match:
              raise SystemExit(f'forbidden source token: {match.group(2)}')
          for token in ('Lean.ofReduceBool', 'native_decide', 'implemented_by', 'extern'):
              if token in source:
                  raise SystemExit(f'forbidden source token: {token}')
          if source.count('#print axioms') != 4:
              raise SystemExit('expected exactly four staged #print axioms commands')
          print('source trust preflight: clean')
          PY

      - name: Compile exact source with AXLE Lean 4.30
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p evidence/rh_robin_energy_gap_drift
          python3 - <<'PY'
          import json
          import pathlib
          import urllib.request

          source = pathlib.Path(
              'verification/rh_robin_energy_gap_drift/RHRobinEnergyGapDrift.lean'
          ).read_text()
          payload = json.dumps({
              'content': source,
              'environment': 'lean-4.30.0',
              'ignore_imports': True,
              'mathlib_options': False,
              'timeout_seconds': 900,
          }).encode()
          request = urllib.request.Request(
              'https://axle.axiommath.ai/api/v1/check',
              data=payload,
              headers={'Content-Type': 'application/json'},
              method='POST',
          )
          with urllib.request.urlopen(request, timeout=960) as response:
              result = json.load(response)

          out = pathlib.Path('evidence/rh_robin_energy_gap_drift/axle-response.json')
          out.write_text(json.dumps(result, indent=2, sort_keys=True))
          print(json.dumps(result, indent=2))

          lean_errors = result.get('lean_messages', {}).get('errors', [])
          lean_warnings = result.get('lean_messages', {}).get('warnings', [])
          tool_errors = result.get('tool_messages', {}).get('errors', [])
          tool_warnings = result.get('tool_messages', {}).get('warnings', [])
          failed = result.get('failed_declarations', [])
          if (not result.get('okay') or lean_errors or lean_warnings
                  or tool_errors or tool_warnings or failed):
              raise SystemExit('Lean/AXLE verification failed or emitted warnings')
          rendered = json.dumps(result)
          for forbidden in ('sorryAx', 'Lean.ofReduceBool'):
              if forbidden in rendered:
                  raise SystemExit(f'postcompile forbidden dependency: {forbidden}')
          print('VERIFIED: exact warning-free finite source compiled under AXLE Lean 4.30')
          PY

      - name: Record replay metadata
        shell: bash
        run: |
          set -euo pipefail
          {
            echo "repository=${GITHUB_REPOSITORY}"
            echo "ref=${GITHUB_REF}"
            echo "commit=${GITHUB_SHA}"
            echo "runner_name=${RUNNER_NAME}"
            echo "runner_os=${RUNNER_OS}"
            echo "source_blob=$(git hash-object verification/rh_robin_energy_gap_drift/RHRobinEnergyGapDrift.lean)"
            echo "source_sha256=$(sha256sum verification/rh_robin_energy_gap_drift/RHRobinEnergyGapDrift.lean | awk '{print $1}')"
            echo "environment=lean-4.30.0"
          } > evidence/rh_robin_energy_gap_drift/replay-metadata.txt

      - name: Upload exact compiler evidence
        uses: actions/upload-artifact@v4
        with:
          name: rh-robin-energy-gap-drift-public-evidence
          path: evidence/rh_robin_energy_gap_drift/
          if-no-files-found: error
