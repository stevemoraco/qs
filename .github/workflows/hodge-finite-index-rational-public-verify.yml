name: Hodge Finite-Index Rational Firewall Lean

on:
  push:
    branches:
      - agent/hodge-finite-index-rational-public-verify-20260811
    paths:
      - 'verification/hodge-finite-index-rational/**'
      - '.github/workflows/hodge-finite-index-rational-public-verify.yml'
  pull_request:
    branches: [main]
    paths:
      - 'verification/hodge-finite-index-rational/**'
      - '.github/workflows/hodge-finite-index-rational-public-verify.yml'
  workflow_dispatch:

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4

      - name: Reject explicit placeholders and custom axioms
        shell: bash
        run: |
          set -euo pipefail
          file=verification/hodge-finite-index-rational/HodgeFiniteIndexRationalFirewall.lean
          python3 - <<'PY'
          import pathlib, re
          p = pathlib.Path('verification/hodge-finite-index-rational/HodgeFiniteIndexRationalFirewall.lean')
          text = p.read_text()
          text = re.sub(r'/-.*?-/', '', text, flags=re.S)
          text = re.sub(r'--.*$', '', text, flags=re.M)
          patterns = [r'^\s*(sorry|admit)\b', r'^\s*axiom\b']
          for pattern in patterns:
              m = re.search(pattern, text, flags=re.M)
              if m:
                  raise SystemExit(f'forbidden declaration: {m.group(0)!r}')
          PY

      - name: Install pinned Lean, fetch pinned Mathlib cache, and compile
        shell: bash
        run: |
          set -euo pipefail
          curl --proto '=https' --tlsv1.2 -sSf \
            https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh \
            | sh -s -- -y --default-toolchain leanprover/lean4:v4.32.1
          export PATH="$HOME/.elan/bin:$PATH"
          lean --version
          lake --version
          cd verification/hodge-finite-index-rational
          lake update
          lake exe cache get
          lake env lean HodgeFiniteIndexRationalFirewall.lean \
            2>&1 | tee lean-output.log
          if grep -Eq 'declaration uses .sorry.|sorryAx|admit' lean-output.log; then
            echo 'Forbidden placeholder axiom found in compiler output.' >&2
            exit 1
          fi
          echo 'VERIFIED finite Hodge finite-index rational firewall core.'

      - name: Upload compiler evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: hodge-finite-index-rational-firewall-lean-evidence
          path: verification/hodge-finite-index-rational/lean-output.log
          if-no-files-found: warn
