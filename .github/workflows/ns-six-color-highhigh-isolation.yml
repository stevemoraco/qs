name: NS Six-Color High-High Isolation Finite Core

on:
  push:
    branches: [agent/ns-six-color-highhigh-isolation-verifier-20260813]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ns-six-color-highhigh-isolation-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Reject explicit placeholders and custom result axioms
        shell: bash
        run: |
          set -euo pipefail
          files=(
            verification/ns-six-color-highhigh-isolation/SixColorHighHighIsolationFinite.lean
            verification/ns-six-color-highhigh-isolation/OuterSidebandNoSlavingFinite.lean
          )
          if grep -En '(^|[^[:alnum:]_])(sorry|admit|axiom|opaque|unsafe|native_decide)([^[:alnum:]_]|$)|Lean\.ofReduceBool' "${files[@]}"; then
            echo 'Forbidden proof placeholder or trust shortcut found.' >&2
            exit 1
          fi
      - name: Install pinned Lean, fetch pinned Mathlib cache, and compile
        shell: bash
        run: |
          set -euo pipefail
          curl --proto '=https' --tlsv1.2 -sSf \
            https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh \
            | sh -s -- -y --default-toolchain leanprover/lean4:v4.32.1
          export PATH="$HOME/.elan/bin:$PATH"
          cd verification/ns-six-color-highhigh-isolation
          lake update
          lake exe cache get
          : > lean-output.log
          for file in SixColorHighHighIsolationFinite.lean OuterSidebandNoSlavingFinite.lean; do
            lake env lean -DmaxHeartbeats=2000000 "$file" 2>&1 | tee -a lean-output.log
          done
          if grep -Eq 'declaration uses .sorry.|sorryAx|admit|Lean\.ofReduceBool' lean-output.log; then
            echo 'Forbidden placeholder axiom found in compiler output.' >&2
            exit 1
          fi
          echo 'VERIFIED finite six-color high-high isolation and no-slaving cores.'
      - name: Upload compiler evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ns-six-color-highhigh-isolation-lean-evidence
          path: verification/ns-six-color-highhigh-isolation/lean-output.log
          if-no-files-found: warn
