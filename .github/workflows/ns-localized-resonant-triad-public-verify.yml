name: NS localized resonant triad public verifier

on:
  push:
    branches:
      - agent/ns-localized-resonant-triad-public-verifier-20260813
  pull_request:
    paths:
      - 'verification/ns-localized-resonant-triad-public/**'
      - '.github/workflows/ns-localized-resonant-triad-public-verify.yml'

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - name: Verify trust boundary and exact source blob
        shell: bash
        run: |
          set -euo pipefail
          dir='verification/ns-localized-resonant-triad-public'
          file="$dir/NSLocalizedResonantTriadCore.lean"
          actual_blob="$(git hash-object "$file")"
          expected_blob='46a0bb40ec1c639bb85c6e4d2181c84e01b97ef9'
          test "$actual_blob" = "$expected_blob"
          printf 'canonical_rh_lean_blob=%s\n' "$actual_blob" | tee "$dir/source-blob.txt"
          sha256sum "$file" | tee "$dir/source-sha256.txt"
          if grep -En '(^|[^[:alnum:]_])(sorry|admit|sorryAx)([^[:alnum:]_]|$)' "$file"; then
            echo 'Forbidden proof placeholder found.' >&2
            exit 1
          fi
          if grep -En '^[[:space:]]*(axiom|opaque|unsafe)[[:space:]]' "$file"; then
            echo 'Forbidden trust declaration found.' >&2
            exit 1
          fi
          if grep -En '(native_decide|Lean\.ofReduceBool)' "$file"; then
            echo 'Forbidden computational trust escape found.' >&2
            exit 1
          fi

      - name: Install pinned Lean and compile exact source
        shell: bash
        run: |
          set -euo pipefail
          dir='verification/ns-localized-resonant-triad-public'
          curl --proto '=https' --tlsv1.2 -sSf \
            https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh \
            | sh -s -- -y --default-toolchain leanprover/lean4:v4.32.1
          export PATH="$HOME/.elan/bin:$PATH"
          lean --version | tee "$dir/lean-version.txt"
          lake --version | tee -a "$dir/lean-version.txt"
          cd "$dir"
          lake update
          lake exe cache get
          set +e
          lake env lean NSLocalizedResonantTriadCore.lean > lean-output.log 2>&1
          status=$?
          set -e
          cat lean-output.log
          if [ "$status" -ne 0 ]; then
            echo "Lean compilation failed with status $status" >&2
            exit "$status"
          fi
          if grep -Eq 'declaration uses .sorry.|sorryAx|admit|Lean.ofReduceBool' lean-output.log; then
            echo 'Forbidden dependency found in compiler report.' >&2
            exit 1
          fi
          echo 'VERIFIED NS localized resonant triad finite core.'

      - name: Upload verification evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ns-localized-resonant-triad-public-evidence
          path: |
            verification/ns-localized-resonant-triad-public/source-blob.txt
            verification/ns-localized-resonant-triad-public/source-sha256.txt
            verification/ns-localized-resonant-triad-public/lean-version.txt
            verification/ns-localized-resonant-triad-public/lean-output.log
