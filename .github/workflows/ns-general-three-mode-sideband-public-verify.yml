name: NS general three-mode sideband public verifier

on:
  push:
    branches:
      - agent/ns-general-three-mode-sideband-public-verifier-20260813
  pull_request:
    paths:
      - 'verification/ns-general-three-mode-sideband-public/**'
      - '.github/workflows/ns-general-three-mode-sideband-public-verify.yml'

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - name: Verify exact private-source identity and trust boundary
        shell: bash
        run: |
          set -euo pipefail
          dir='verification/ns-general-three-mode-sideband-public'
          file="$dir/GeneralThreeModeSidebandNoGo.lean"
          actual_blob="$(git hash-object "$file")"
          expected_blob='fce9a53026fc6ac0d87efc270a72a0b2ded13864'
          test "$actual_blob" = "$expected_blob"
          printf 'canonical_private_blob=%s\n' "$actual_blob" | tee "$dir/source-blob.txt"
          sha256sum "$file" | tee "$dir/source-sha256.txt"
          if grep -En '(^|[^[:alnum:]_])(sorry|admit|sorryAx)([^[:alnum:]_]|$)' "$file"; then
            echo 'Forbidden proof placeholder found.' >&2
            exit 1
          fi
          if grep -En '^[[:space:]]*(axiom|opaque|unsafe)[[:space:]]' "$file"; then
            echo 'Forbidden trust declaration found.' >&2
            exit 1
          fi
          if grep -En '(native_decide|Lean\.ofReduceBool)' "$file"; then
            echo 'Forbidden computational trust escape found.' >&2
            exit 1
          fi

      - name: Install pinned Lean and compile exact source
        shell: bash
        run: |
          set -euo pipefail
          dir='verification/ns-general-three-mode-sideband-public'
          curl --proto '=https' --tlsv1.2 -sSf \
            https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh \
            | sh -s -- -y --default-toolchain leanprover/lean4:v4.32.1
          export PATH="$HOME/.elan/bin:$PATH"
          lean --version | tee "$dir/lean-version.txt"
          lake --version | tee -a "$dir/lean-version.txt"
          cd "$dir"
          lake update
          lake exe cache get
          set +e
          lake env lean GeneralThreeModeSidebandNoGo.lean > lean-output.log 2>&1
          status=$?
          set -e
          cat lean-output.log
          if [ "$status" -ne 0 ]; then
            echo "Lean compilation failed with status $status" >&2
            exit "$status"
          fi
          if grep -Eq 'declaration uses .sorry.|sorryAx|admit|Lean.ofReduceBool' lean-output.log; then
            echo 'Forbidden dependency found in compiler report.' >&2
            exit 1
          fi
          echo 'VERIFIED general three-mode sideband no-go finite algebra.'

      - name: Upload verification evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ns-general-three-mode-sideband-public-evidence
          path: |
            verification/ns-general-three-mode-sideband-public/source-blob.txt
            verification/ns-general-three-mode-sideband-public/source-sha256.txt
            verification/ns-general-three-mode-sideband-public/lean-version.txt
            verification/ns-general-three-mode-sideband-public/lean-output.log
